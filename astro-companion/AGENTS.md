# astro-agent-spec-writer

descrBe what an agent should do, it writes a structured agent spec (name, description, capabilities, example, prompts) ready for the registry.

For comprehensive documentation including **critical API usage notes**, run `ast docs`.
