# yaml-language-server: $schema=https://astropods.com/schema/package.json
spec: blueprint/v1
name: hello-astro

agent:
  build:
    context: .
    dockerfile: Dockerfile

dev:
  interfaces:
    messaging:
      adapters: [web]
